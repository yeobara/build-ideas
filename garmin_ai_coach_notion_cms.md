# 🚀 아이디어: Notion-Driven AI Coach (Headless CMS 아키텍처)

## 📌 배경 및 목적
현재 Garmin AI Coach 시스템은 파이썬 스크립트 내부(Git 저장소)에 프롬프트(오사코 스구루 스타일 코칭 철학 등)와 분석 로직이 하드코딩되어 있습니다. 
이를 개선하여 코딩을 모르는 사용자도 손쉽게 자신만의 AI 코치를 구축하고, 하루의 모든 훈련(수면, 헬스, 러닝)을 '하나의 유기체'처럼 통합 분석할 수 있는 **노션 기반의 확장 가능한 SaaS형 아키텍처**로 업그레이드하는 아이디어입니다.

## 🏛️ 아키텍처 핵심 구조 (노션의 Headless CMS화)

### 1. 프롬프트와 뇌(Brain)의 분리 🧠
- 파이썬 코드 안에 있던 모든 '프롬프트 지시사항'을 지우고, 노션의 **[AI 코치 설정 페이지]**로 100% 옮깁니다.
- 파이썬 스크립트 실행 시, 노션 API를 통해 이 페이지의 텍스트를 읽어와 LLM(Gemini)의 시스템 프롬프트로 주입합니다.
- **효과:** 코딩 지식이 전혀 없는 사람도 스마트폰 노션 앱에서 텍스트만 고치면 AI 코치의 성향(예: 킵초게 스타일로 변경, 스파르타 모드 등)을 즉시 바꿀 수 있습니다.

### 2. 마스터 대시보드 (유기적 기억 장치) 🧬
- 기존에 파편화되어 있던 리포트(수면, 헬스, 러닝)를 노션의 **[Daily Master Dashboard]** 하나로 묶습니다.
- **관계형(Relation) DB 활용:** '오늘'이라는 하나의 행(Row)에 아침 수면, 점심 헬스, 저녁 러닝 기록이 롤업(Rollup)되어 한눈에 보입니다.
- **AI 기억 연동(Memory):** 저녁 러닝을 분석할 때, 파이썬 코드가 아침 수면 점수와 점심 헬스 코칭 피드백을 노션에서 읽어와 AI에게 전달합니다. 
- **효과:** AI가 *"아침에 수면 점수가 낮았고 점심에 하체 보강까지 강하게 하셔서 오늘 러닝 케이던스가 떨어진 것 같습니다"* 와 같이 진짜 사람 코치처럼 인과관계를 꿰뚫어 보는 통합 피드백을 제공합니다.

### 3. 최소화된 Git 코드 (단순 운반책) 📦
- Git 저장소에 남는 파이썬 코드는 복잡한 조건문 없이 매우 단순(Dumb)해집니다.
- 오직 1) 가민 데이터 수집, 2) 노션 프롬프트 및 과거 기억 읽기, 3) Gemini API 쏴주기, 4) 노션에 최종 업로드라는 '배달(Pipeline)' 역할만 수행합니다.
- **확장성(비즈니스화):** 다른 사용자들은 회원님의 노션 템플릿만 복사(Duplicate)하고 Github에 API 키만 넣으면 자신만의 AI 코치 시스템을 10분 만에 세팅할 수 있게 됩니다. 추후 오픈소스나 템플릿 비즈니스로 확장하기 완벽한 구조입니다.

## 📝 액션 플랜 (추후 구현 시)
1. **노션 환경 구축:** 노션에 `Daily Master Dashboard` DB와 `AI 코치 프롬프트 설정` 페이지 세팅 (현재 ID 값 추출 완료)
2. **코드 리팩토링:** `notion_writer.py`에 노션 페이지 텍스트를 읽어오는 `Fetch` 기능과, 오늘자 과거 기록을 읽어오는 `Memory` 기능 추가
3. **프롬프트 분리:** 기존 `gym_reporter.py`, `running_reporter.py` 등에서 하드코딩 프롬프트 제거 및 노션 연동 로직 이식
